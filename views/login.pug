extends layout

block content
    div.main
        div.imgbox
            img.imglogo(src='/pictures/logo_transparent.png' alt='logo')
        div.alignInput
            div.inputwrapper
                img.inputicon(src='/pictures/user.png' alt='mail icon')
                input#username.inputfield(type="text", placeholder="Username", required='true',maxlength="255")
            div.inputwrapper
                img.inputicon(src='/pictures/password.png' alt='mail icon')
                input#password.inputfield(type="password", placeholder="Password", required='true',maxlength="255")
        div.alignWrapper
            div.btnWrapper
                label#lError.errorField() &nbsp
            div.btnWrapper
                button#btnLogin.btnAuth() Login
            div.btnWrapper
                a.register(class='button', href='/registration') Have no Account?
    script(src='javascripts/login.js')